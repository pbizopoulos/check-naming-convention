# Naming convention for python

# Requirements
POSIX Utilities
Git
Docker

# Download
git clone https://github.com/pbizopoulos/naming-convention
cd naming-convention/

# Development
cd python/python/ && make clean check all && cd ../..
cd python/ && make clean check all && cd ..

# Installation
pip install https://github.com/pbizopoulos/naming-convention/tarball/main#subdirectory=python/python

# Usage
naming_convention main.py

