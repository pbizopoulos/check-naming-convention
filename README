# Check naming convention for python
https://pbizopoulos.github.io/check-naming-convention

# Requirements
POSIX Shell & Utilities
Git
Docker

# Download
git clone https://github.com/pbizopoulos/check-naming-convention $HOME/github.com/pbizopoulos/check-naming-convention
cd $HOME/github.com/pbizopoulos/check-naming-convention/

# Development
cd docs/ && make clean check all && cd ..
cd python/python/ && make clean check all && cd ../..
cd python/ && make clean check all && cd ..

# Staging
cd docs/ && make STAGING=1

# Installation
pip install https://github.com/pbizopoulos/check-naming-convention/tarball/main#subdirectory=python

# Usage
check_naming_convention main.py

